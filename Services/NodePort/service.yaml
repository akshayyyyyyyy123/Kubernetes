## NodePort Service ##

apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  namespace: nodeport-ns
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 1733        # Service Port
    nodePort: 32111   # NodePort 
    targetPort: 80    # App Port


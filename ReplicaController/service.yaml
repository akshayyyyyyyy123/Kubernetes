## ClusterIP service ##

apiVersion: v1
kind: Service
metadata:
  name: replication-controller-svc
  namespace: replication-controller
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80

## ClusterIP service ##

apiVersion: v1
kind: Service
metadata:
  name: replicaset-svc
  namespace: replicaset
spec:
  type: ClusterIP
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
